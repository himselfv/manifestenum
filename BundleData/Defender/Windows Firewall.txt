# Windows Firewall
# Requires and is based on Windows Filtering Platform (base filtering engine). The core is implemented there (Base Filtering Engine), this only hosts user-mode application and support tools.

Networking-MPSSVC*
